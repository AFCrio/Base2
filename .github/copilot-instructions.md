# Copilot Instructions

## Рекомендации по проекту
- For order assignments, analytics/search only needs PersonId, WeaponId, VehicleId. Snapshot fields like rank/position/ammo details are unnecessary; these should be stored only as rendered text (anonymized, no analytics needed).
